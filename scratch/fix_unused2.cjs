const { Project, Node, SyntaxKind } = require('ts-morph'); const project = new Project({ tsConfigFilePath: 'tsconfig.json' }); const diags = project.getPreEmitDiagnostics(); let changed = 0; diags.forEach(d => { if(d.getCode() === 6133) { const node = d.getStart() ? d.getSourceFile().getDescendantAtPos(d.getStart()) : null; if(node) { try { if (Node.isIdentifier(node)) { const parent = node.getParent(); if (Node.isImportSpecifier(parent) || Node.isImportClause(parent)) { parent.remove(); changed++; } else if (Node.isParameterDeclaration(parent) || Node.isVariableDeclaration(parent) || Node.isBindingElement(parent)) { const name = node.getText(); if(!name.startsWith('_')) { node.replaceWithText('_' + name); changed++; } } } } catch(e) {} } } }); project.saveSync(); console.log('Changed ' + changed + ' items.');
