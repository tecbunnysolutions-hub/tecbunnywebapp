import { MobileOperationsDashboard } from '@/components/mobile-operations-dashboard';

export const metadata = {
  title: 'Mobile Operations - TecBunny',
  description: 'Sales operations dashboard optimized for mobile devices',
};

export default function MobileOperationsPage() {
  return <MobileOperationsDashboard />;
}
